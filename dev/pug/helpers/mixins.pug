mixin icon(name,mod)
	if(mod)
		svg(class="sprite-icon icon-" + name + ' ' + mod)
			use(xlink:href="img/sprite/symbol/sprite.svg#" + name)
	else
		svg(class="sprite-icon icon-" + name)
			use(xlink:href="img/sprite/symbol/sprite.svg#" + name)
